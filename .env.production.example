# Copy to .env on the server: cp .env.production.example .env

# Public URL (used for CORS) — replace with your server IP or domain
# Examples:
#   http://203.0.113.10:3314
#   https://aveno.example.com
PUBLIC_URL=http://YOUR_SERVER_IP:3314

# Port exposed on the host (frontend + API proxy via Next.js)
APP_PORT=3314

# Database (change password!)
POSTGRES_USER=void
POSTGRES_PASSWORD=change-this-strong-password
POSTGRES_DB=void_spirit

# API secret (generate: openssl rand -hex 32)
SECRET_KEY=change-this-secret-key

# JWT session length — 525600 minutes = 1 year
ACCESS_TOKEN_EXPIRE_MINUTES=525600

# Iran mirrors during Docker build (true on Iranian servers)
USE_IRAN_MIRRORS=true
NPM_REGISTRY=https://mirror-npm.runflare.com
