# Runflare NPM mirror (Iran) — primary for server builds
registry=https://mirror-npm.runflare.com
strict-ssl=false

# Fallback: uncomment if Runflare is unavailable
# registry=https://package-mirror.liara.ir/repository/npm/
